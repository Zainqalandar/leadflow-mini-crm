# Angular does not load secrets from environment files.
# Local development automatically uses: http://localhost:5000/api
# Hosted builds automatically use the Render API URL configured in:
# src/app/leadflow-api.service.ts
#
# Keep API credentials in the API service environment, never in this frontend.
