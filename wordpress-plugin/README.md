# LeadFlow Connector

Initial WordPress plugin scaffold for the LeadFlow Mini CRM assessment.

The plugin is mounted into the local Docker WordPress container by the root `docker-compose.yml` file. The form, admin lead screen, validation, nonce protection, and API sync will be implemented here next.
